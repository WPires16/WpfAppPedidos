<Window x:Class="WpfAppPedidos.Views.ProdutoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppPedidos.Views"
        mc:Ignorable="d"
        Title="Cadastro de Produtos" Height="550" Width="850" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" >
            <Label Content="Nome:" VerticalAlignment="Center" />
            <TextBox Width="150" Margin="5,0" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Código:" VerticalAlignment="Center" />
            <TextBox Width="120" Margin="5,0" Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Valor Min:" VerticalAlignment="Center" />
            <TextBox Width="80" Margin="5,0" Text="{Binding FiltroValorMin, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Valor Max:" VerticalAlignment="Center" />
            <TextBox Width="80" Margin="5,0" Text="{Binding FiltroValorMax, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Grid Produtos -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Produtos}" SelectedItem="{Binding ProdutoSelecionado}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botões -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" >
            <Button Content="Incluir" Width="100" Margin="5" Command="{Binding IncluirCommand}" />
            <Button Content="Excluir" Width="100" Margin="5" Command="{Binding ExcluirCommand}" />
            <Button Content="Salvar" Width="100" Margin="5" Command="{Binding SalvarCommand}" />
        </StackPanel>
    </Grid>
</Window>
